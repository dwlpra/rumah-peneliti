{
  "name": "rumahpeneliti-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for RumahPeneliti.com micropayment",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network zeroTestnet",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network zeroMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
